import React from 'react';

const ManageFamily = () => {
    const styles = {
        root: {
            width: "60%",
            margin: "auto",
            background: "white",
        }
    }
    return (
        <div style={styles.root}>
            family
        </div>
    );
}

export default ManageFamily;